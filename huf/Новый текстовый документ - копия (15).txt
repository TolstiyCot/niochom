public string generateEMail()
{
    if (PersonName.Length > 20)
    {
        PersonName = PersonName.Substring(0, 20);
    }
    string[] names = PersonName.Split(' ');
    return names[0] + "." + names[1] + "@domain.ua";
}