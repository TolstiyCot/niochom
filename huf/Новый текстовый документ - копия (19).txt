string mailTo = getMail();

static string getMail()
{
    string check = Config.GetSetting("AdminNotifications_EmailAddress");
    if (check != null)
    {
        return check;
    }
    else
    {
        return Globals.GetHostPortalSettings().HostSettings["SMTPPassword"].ToString();
    }
}